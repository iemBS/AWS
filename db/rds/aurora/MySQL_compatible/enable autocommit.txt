Enable Autocommit
Main Success Scenario:
  1. see if autocommit already enabled
    select @@autocommit;
  2. set autocommit = 1;
  3. end
Alternatives:
  1a. autocommit already enabled 
    1a1. go to step 3
    
